<Window x:Class="MVXTester.App.Views.CodePreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generated Code" Width="800" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="LanguageLabel" FontWeight="Bold" FontSize="14"
                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />

        <TextBox Grid.Row="1" x:Name="CodeTextBox"
                 IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12"
                 Background="{DynamicResource SurfaceBrush}"
                 Foreground="{DynamicResource ForegroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Copy to Clipboard" Click="CopyClick" Margin="0,0,8,0" Padding="12,6" />
            <Button Content="Save As..." Click="SaveClick" Margin="0,0,8,0" Padding="12,6" />
            <Button Content="Close" Click="CloseClick" Padding="12,6" />
        </StackPanel>
    </Grid>
</Window>
