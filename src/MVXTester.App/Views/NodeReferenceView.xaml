<UserControl x:Class="MVXTester.App.Views.NodeReferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Expander style for category sections -->
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20,16">
        <StackPanel x:Name="MainPanel">
            <!-- Title -->
            <TextBlock Text="Node Reference" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4" />
            <TextBlock Text="각 노드의 포트, 속성, 기능, 응용 분야에 대한 상세 레퍼런스입니다. 카테고리를 클릭하여 펼칠 수 있습니다."
                       FontSize="11.5" Foreground="{DynamicResource ForegroundDimBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,16" />
            <!-- Dynamic content is added by code-behind -->
        </StackPanel>
    </ScrollViewer>
</UserControl>
